

echo "코드 포맷 확인..."
npm run format:check
if [ $? -ne 0 ]; then
  echo "❌ 포맷 오류 - 푸시 취소"
  echo "다음 명령어로 수정 후 다시 푸시하세요:"
  echo "npm run format"
  exit 1
fi

echo "푸시 전 빌드 테스트 진행"
npm run build

if [ $? -eq 0 ]; then
  echo "빌드 성공 - 푸시 진행.."
else
  echo "빌드 실패 - 푸시 취소.."
  exit 1
fi