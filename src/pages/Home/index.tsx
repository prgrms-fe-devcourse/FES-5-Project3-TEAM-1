import { ModalProvider } from '@/shared/utils/ModalProvider';

const Home = () => {
  return (
    <ModalProvider>
      <div className="p-4 flex flex-col gap-2"></div>
    </ModalProvider>
  );
};
export default Home;
